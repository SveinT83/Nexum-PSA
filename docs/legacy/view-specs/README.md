# Legacy View Specs

This folder contains old Markdown planning and view specification files that previously lived inside production view paths.

They were moved here during beta cleanup so `resources/views` and module `Views` directories contain renderable UI files only.

These files are historical reference material. Before using one as source of truth, compare it with current module documentation, RFCs, feature slices, tests, and implemented code.
